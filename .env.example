# 不会自动加载。编辑后使用 `set -a; source .env; set +a`，或写入服务环境。
# 不要提交填好的 .env、cookie、API key、私有授权或声纹。

# === CPA：真实文字/视觉流程需要自己的服务 ===
# 这些占位值不是可用服务；先在自己的网关确认调用点所需模型/能力。
CPA_BASE_URL=https://your-cpa-host/v1
CPA_API_KEY=your-key
# 包装器默认 gpt-6-sol；部分调用点显式指定模型/effort，不全部受此覆盖。
# CPA_CHAT_MODEL=gpt-6-sol
# 健康探针固定使用 gpt-6-luna；Astra 仅允许显式调用，不作自动回退。

# === 工作目录与源目录 ===
# 工作根不是录播目录。参考默认值如下，个人试用可改成自己的绝对路径。
AUTOSLICE_BASE=/opt/bilive/autoslice
# 首次部署应显式指向真实录播目录，不沿用参考 CloudDrive 布局。
# AUTOSLICE_REC_ROOT=/absolute/path/to/recordings
# AUTOSLICE_CANONICAL_REC_ROOT=/absolute/path/to/canonical-recordings
# BILIVE_VIDEOS_ROOT=/absolute/path/to/recorder-output

# === 频道 ===
# 默认 lidousha 是公开示例；换频道先建立 profiles/<id>/profile.json 和资产。
# import 时读取，修改后重新启动进程。pytest 时不要设置此变量。
# AUTOSLICE_PROFILE=your-channel
# 示例片头媒体不分发；试跑示例时关闭。自己的频道按 profile 政策配置。
# AUTOSLICE_BRANDING_INTRO=off
# AUTOSLICE_EMOTE_DIR=/absolute/path/to/your-emotes

# === 局部音频见证 / 歌切 ===
# 普通链是 BCUT → CPA 文字先判，需要听音时才补局部证据；不是整片 AGY 替代 BCUT。
# AGY CLI 与 Gemini API 是不同入口，模型标识、能力、额度要分别验证。
# 缺少服务时相应门可能拒绝；配上 key 不表示每个入口必然允许后备。
# AGY_BIN=/absolute/path/to/agy
# ENTITY_AUDIO_AGY_MODEL="Gemini 3.6 Flash (High)"
# ENTITY_AUDIO_GEMINI_API_MODEL=gemini-3.6-flash
GEMINI_API_KEY=
GEMINI_KEY_BACKUP=
# 示例明确禁止付费后备；0 不免除主服务本身可能产生的费用。
GEMINI_PAID_BACKUP_DAILY_CAP=0
# 参考 runner 另读 /opt/bilive/.env 的 Gemini key、$AUTOSLICE_BASE/cpa.env 的 CPA key。
# 自定义布局必须确认当前入口实际加载的环境，不把仓库 .env 当成已自动生效。

# === 仅发布 / 稿件维护需要，制作审阅包不需要 ===
# 使用自己的账号、简介和合集。上传前还需独立授权与出版登记。
# AUTOSLICE_UPLOAD_DESC="你的频道简介与来源"
# AUTOSLICE_FALLBACK_TAGS=虚拟主播,直播切片
# 合集/小节 ID 无公共默认值；以下 0 只是结构示例，不是可用授权。
# AUTOSLICE_SEASON_IDS={"talk": {"season_id": 0, "section_id": 0}, "song": {"season_id": 0, "section_id": 0}}
# 获取方式：在创作中心把自己的稿件加入目标合集，再用 bili_archive_tool.py view 回读。
# cookie 存在仓库外，位置与只读检查见 docs/credentials.md。
