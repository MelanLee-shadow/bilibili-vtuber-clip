# 安全政策

## 报告漏洞

请**不要**用公开 issue 报告安全问题。走 GitHub 的私密披露通道：仓库
**Security → Report a vulnerability**（Security Advisories）。

本项目由个人维护，会尽力及时回应；修复以补丁发布加披露说明的方式进行。

## 运行数据与问题附件

转写缓存、`.pronoun-trace/`、字幕中间稿、发布 manifest／回执和台账都可能包含真实
录播、用户名、源路径或私有语境。它们属于部署者的运行数据，不是应提交到公共软件仓库的
产品源文件。保留恢复所需的私有备份；公开复现请使用自己构造的合成输入。

诊断不主动复制凭据环境或完整传输异常，但不能保证消除任意嵌在原文中的秘密。
文件权限和忽略规则都不等于公开安全检查：忽略规则不会移除已跟踪文件；提交或附加
任何日志前都应检查实际内容。不要把“存在哈希”误当成“文件已脱敏”。

## 范围说明

- 本项目是**自托管**流水线：没有托管服务、没有我们持有的用户数据。全部
  凭据（B 站 cookie、LLM key）都在部署方自己手里，仓库与测试套件不含任何
  真实凭据（测试有密闭守卫，凭据在场也不会外呼）。
- 特别欢迎的报告类型：任何能让流水线**绕过 fail-closed 门**（字幕真值、
  出版登记、上传授权）的路径；凭据可能被写入产物/日志的路径；依赖链漏洞。
