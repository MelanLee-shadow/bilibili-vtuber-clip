# 默认安装以下 Python 依赖；声纹栈含 torch，体积较大。
# 安装依赖不提供 ffmpeg、模型权重、频道资产、服务凭据或发布授权。
# 裁减可选组前先核对实际入口依赖；pip check 不代替功能或全套测试。

# —— 核心（转写/校对/标题/封面渲染/测试）——
pillow>=10
pypinyin>=0.50
pytest>=8

# —— 弹幕/礼物/SC 备份监听（scripts/danmaku_backup_listener.py）——
websocket-client>=1.6
brotli>=1.1

# —— VAD 语音跨度导出（scripts/silero_vad_spans.py，字幕时间轴 QA）——
numpy>=1.26
onnxruntime>=1.17

# —— 声纹说话人确认（CAM++；src/autoslice/speaker_finalizer.py /
#     host_vocal_proof.py。首次运行由 modelscope 自动下载模型）——
torch>=2.1
soundfile>=0.12
modelscope>=1.20
