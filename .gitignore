# 运行时状态、报告、缓存和诊断属于每个部署自己的私有数据，不入公开源码仓。
reports/**
.autoslice/
out/
state/
logs/
.venv/
cpa.env

# 字幕/ASR 阶段的内容寻址缓存和逐次诊断可能包含原字幕、用户名、模型回答或私有语境。
**/*.pronoun-trace/
**/.transcription-stage-cache/
**/*.transcription-reuse.json
**/*.asr-*.raw.json
**/*.asr-source.json
**/*.asr_draft.srt
**/*.cpa-reviewed.srt
**/*.pre-cpa.srt
**/*.pre-cpa-audit.json
**/*.fidelity-audit.json
**/*.screen_text.json

# 临时清理计划；已审核、确需公开的产品模板必须位于 assets/_template 等受管源码路径。
cleanup_manifests/*.tmp

# profile 输出目录（本地交付/媒体拉取，不入库；目录名 = 各 profile 的 output_directory）
lidousha/**

# 媒体永不入库
*.mp4
*.flv
*.m4s
*.part
*.ts
*.mov

# Secrets
.env
.env.*
!.env.example

# 缓存与工具产物
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.cache/

# OS/编辑器噪音
.DS_Store
*.swp
*.swo

# 部署备份
*.bak-*
*.backup

# 表情包媒体随仓发布（assets/<profile>/emote/，示例频道官方表情包）；
# 换频道时用 AUTOSLICE_EMOTE_DIR 指向你自己的媒体目录。
