# 90 授权上传与发布

**本步权威在 skill：`.agent/skills/bilive-autoslice-publish/SKILL.md`**（2026-07-04 全面修订版）。
本文件只放指针，不复制规则。

- 上传永远需要 Ivan 对每条明确授权；授权上传的内容必须 commit（memory `authorized-upload-must-commit`）。
- 标题前缀/简介两行/标签口径/合集入集/公开验证/API 坑：全在 publish skill。
- 换源/编辑置换：memory `bilibili-edit-replace-playbook`。
