# 本文件不会被自动加载：跑脚本前 `set -a; source .env; set +a`，或把变量写进
# 服务/cron 环境。无人值守 runner 的参考部署另读 $AUTOSLICE_BASE/cpa.env。

# ===== 必填 =====

# LLM 两条独立的腿：
# 1) GPT 系列（语义裁决/标题/封面）经 CPA 统一入口
#    （推荐自建 CLIProxyAPI：https://github.com/luispater/CLIProxyAPI）；
CPA_BASE_URL=https://your-cpa-host/v1
CPA_API_KEY=your-key

# 2) 听音腿（Gemini 系列，转写精修/声学听写）：**AGY 订阅与 API key 二选一**
#    ——它们是同一个模型（gemini-3.6 系）的两种接入，配一个就够；都配则按
#    订阅→API 次序自动做配额兜底。API key 直连官方、不经 CPA。付费兜底位可留空。
#    注意：无人值守 runner 的参考部署从 /opt/bilive/.env 读 GEMINI 系列 key；
#    非 /opt 布局要把它们 export 进 runner 的进程环境。
GEMINI_API_KEY=
GEMINI_KEY_BACKUP=

# ===== 部署布局 =====

# 管线状态/工作根（state、缓存、交付都在它下面；不是录播目录）。
AUTOSLICE_BASE=/opt/bilive/autoslice

# 录播根目录（BililiveRecorder 输出；按 <room_id>/ 分房间目录）。
# BILIVE_VIDEOS_ROOT=/opt/bilive/Videos

# runner 扫段的录制根（默认是参考部署的 CloudDrive 路径——**新部署几乎必须
# 自己设**，指向真实存放 <room>_YYYYMMDD-HH-MM-SS.* 的目录树）。
# AUTOSLICE_REC_ROOT=/path/to/live-recordings/<room_id>
# 封口后段的 canonical 根（与 REC_ROOT 相同布局；不设则用 REC_ROOT）。
# AUTOSLICE_CANONICAL_REC_ROOT=

# ===== 频道 profile =====

# 选择频道 profile（profiles/<id>/profile.json；默认是 lidousha 示例 profile）。
# 管线在进程 import 时读取——启动 runner 前设置，不要在运行中改。
# AUTOSLICE_PROFILE=lidousha

# 上传兜底的频道身份数据。正常发布流程中 tags/简介由 authorized_upload.py 的
# manifest 冻结；这两个只是 do_upload.sh 在 manifest 缺项时的兜底。换频道必须
# 换成你自己的，绝不要用别人频道的简介上传。
# 注意：稿件简介第一行由程序自动加项目署名（"本切片由 bilibili-vtuber-clip
# 项目提供：…"，README 礼节性请求的默认实现）；这里只填你频道自己的行
# （主播主页/直播间链接等），不用重复署名。
# AUTOSLICE_UPLOAD_DESC="你的频道简介（写进稿件简介）"
# AUTOSLICE_FALLBACK_TAGS=虚拟主播,直播切片

# 发布入集校验的合集/小节 ID（账号专属，发布 lane 必填，**无默认值**——
# 未配置时发布闭环会明确报错而不是入错集）。获取：在创作中心手动把任一稿件
# 加入目标合集，再用 scripts/bili_archive_tool.py view 回读 season_id/section_id。
# AUTOSLICE_SEASON_IDS={"talk": {"season_id": 0, "section_id": 0}, "song": {"season_id": 0, "section_id": 0}}

# 听音腿的订阅接入（Google Antigravity 的 CLI）：与上面的 GEMINI_API_KEY
# **二选一**（同一个模型的两种入口；都配则 订阅→API 自动兜底）。
# AGY_BIN=~/.local/bin/agy

# 封面表情包通道的媒体目录。仓内自带示例频道媒体（assets/lidousha/emote/，零配置可用）；
# 换频道时指向你自己的表情包目录。
# AUTOSLICE_EMOTE_DIR=/path/to/emote/media
