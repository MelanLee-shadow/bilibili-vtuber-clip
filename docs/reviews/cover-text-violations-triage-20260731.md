# 封面文字违例分诊单（2026-07-31）

状态：`RESOLVED / NO_REPAIR_NEEDED`（当日终版；首版结论被证伪，见下）
范围：`free:/opt/bilive/autoslice/out/2026-07-2*` 全部 30 条有 `rendered_lines` 的成品
判据：确定性门 `cover_thumbnail_lines_are_readable`（≤2 物理行 且 每行 ≤9em）

## 终版结论：存量零欠账，一条都不用修

离线回归在**产出记录**上命中 6 条违例（24 条合规零回归），但逐条核对**线上真相**后：

| 候选 | 记录里的违例 | 线上真相 | 处置 |
|---|---|---|---|
| `auto_192000_909_1014` 白色奶龙 | 5 行（7/25 原始产出）| 线上是 7/30 cover-only 修复后的 3 行版（CDN sha `8bd5fa4f…` 已核对），Ivan 07-31 亲阅：「已经很好了」 | 已被取代，不修 |
| `auto_142942_496_618` | 6 行（7/26 产出记录）| 线上是 7/30 japanese-pronoun-r7 整片重跑后的 **2 行合规版**（`public-cover-readback.r7.png` 亲验：`一声"偶"让小李 / 日语人称翻译大会`） | 已被取代，不修 |
| `auto_193515_1406_1543` 8行 | 真实 | **从未发布**：free state `review_ready`、无 BV、全库无上传回执 | Ivan 07-31：07-22 过时不修 |
| `auto_193515_233_361` 7行 | 真实 | 同上，未发布 | 同上 |
| `auto_193515_1569_1672` 6行 | 真实 | 同上，未发布 | 同上 |
| `auto_193515_672_909` 3行超宽 | 真实 | 同上，未发布 | 同上 |

新产出自 2026-07-31 的修复链（`f1c018e`/`c80bee1`/`8c63f24`：分行权威等级、
mode=full 回退废除、renderer 背带、120px 版面自愈）起受保护。

## 方法论教训（写给下一次分诊）

**首版分诊单把 `out/*/record.json` 当成了线上状态，两条已重跑替换的稿件被误判为
待修。** `record.json` 是生产当时的快照，不是当前真相；判断线上状态必须读
Creator/public fresh-live 回执或重跑后的最终产物（`reports/authorized_uploads/*`、
`reports/cover_only_repairs/*`、CDN 回下载 sha 比对）。这条其实就是
`docs/HANDOFF.md` 开头那句「`review_ready`、本地 commit、旧 PID 或旧 handoff
都不是发布完成证明」——本单首版没有执行到位。

同理，「不在出版登记 ≠ 未发布」的方向也要双向核实：本案 4 条 `auto_193515_*`
经查确实从未发布（state 无 BV + 全库无回执），登记表是准的；但结论必须来自
core 证据，不能来自登记表的缺席。

## 附带遗留（与封面无关，另行跟踪）

- 无梗字短文案的 120px 版面自愈已落地（`8c63f24`，Ivan 拍板「120px 硬性、
  layout 无所谓、接受版面切换」），banner 选项一节撤销。
